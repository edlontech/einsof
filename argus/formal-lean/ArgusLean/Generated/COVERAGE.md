# ArgusKernel.lean — extraction coverage (Phase B, 2026-05-29)

Generated by `argus/scripts/charon-aeneas-extract.sh` from `argus-kernel` via Charon + Aeneas.

**Status: PARTIAL — does NOT elaborate as-is.** This file is intentionally NOT imported
by `ArgusLean.lean` (the default build target is `ArgusLean/Smoke.lean`, which only proves
spec+Aeneas coexistence). Making this model elaborate is Phase C entry work — see the gaps below.

## Toolchain (Phase B realignment — GO)

| Component | Pin |
|---|---|
| aeneas | `1163128c` (origin/main, Lean backend rebased rc2 → **v4.30.0 stable**) |
| charon | `a0bd2aed` (v0.1.198) |
| aeneas Lean lib | Lean `v4.30.0` + mathlib `v4.30.0` (matches kav/tzimtzum exactly) |
| charon rust nightly | `nightly-2026-02-22` |

- Aeneas Lean library builds green on 4.30.0 **stable** (1684 jobs) — the phase-0 spike's
  `scalar_tac`/`Aeneas.Progress.Progress` blocker does NOT recur on newer main + mathlib 4.30.
- Charon `a0bd2aed` ingests the **full kernel** (no `--start-from` scoping needed) — the
  phase-0 Iterator-trait bug at Charon `43459f88` is fixed. LLBC: `argus/argus_kernel.llbc` (~6.6 MB).

## Transition coverage (12 actions)

| Transition | Extraction | Note |
|---|---|---|
| `register_tool` | transparent `def` | |
| `load_instruction` | transparent `def` | |
| `delegate` | transparent `def` | |
| `grant_capability` | transparent `def` | |
| `revoke` | transparent `def` | |
| `cascade_revoke` | transparent `def` | |
| `invoke_complete` | transparent `def` | |
| `return_endorsed` | transparent `def` | |
| `return_unendorsed` | transparent `def` | |
| `sentinel_refresh_budget` | transparent `def` | |
| **`invoke_start`** | **opaque `axiom`** | Aeneas: *"Returns inside of nested loops are not supported yet"* |
| **`sentinel_elevate_taint`** | **opaque `axiom`** | Aeneas: *"Early returns inside of loops are not supported yet"* |

10/12 transparent, 2 opaque. The two opaque actions are among the most security-critical
(the flow gate + speculative-taint elevation).

## Why it does not elaborate yet (Phase C entry blockers)

These are upstream Aeneas limitations/bugs and translation defects — NOT fillable by
`-split-files` external models alone, and NOT fixed by `-impl-namespace` (both tried 2026-05-29):

1. **Unsupported control flow (gates `invoke_start`, `sentinel_elevate_taint`):** Aeneas does
   not yet support early/`return` inside (nested) loops. Both actions fall back to `axiom`.
   Resolution requires upstream Aeneas support OR refactoring the Rust transitions to hoist
   returns out of loops (modifies the deployed artifact — a Phase C decision).
2. **BTreeSet `.copied()` iterator → "Internal error, please file an issue" / "Region ids
   should not be visited directly":** triggered by `BTreeSet`/slice `Iter::copied`. Cascades
   into `delegate`/`revoke`/`cascade_revoke` when `-split-files` is used (the committed default
   extraction avoids that cascade). Upstream Aeneas bug.
3. **`&'static [CapKind]` → "There should be no bottoms in the value":** `CapKind::all()`
   returns a static slice of all 17 variants; Aeneas can't translate the static data. Also hits
   `CapKind::as_str` / `Display::fmt` (big `match` over the enum).
4. **Iterator builtin model gaps:** `Iterator::collect` / `Iterator::copied` fields missing in
   the Aeneas Lean library's builtin trait model.
5. **Elaboration-level translation defects:** self-projection `state.KernelState.KernelState` /
   `event.KernelEvent.KernelEvent`, inline `sorry` emitted in struct-field position (parse
   error), a `meta` identifier clashing with the Lean keyword, an unknown
   `Aeneas.Std.core.cmp.PartialEq.Blanket.ne`, and closure `call_once`/`call_mut` type mismatches.

## Phase C starting point

Give meaning to the opaque externals (BTreeSet ops, `CapKind::all`, `cmp` blanket) via
`-split-files` template files (`*External_Template.lean`), and resolve the two unsupported-loop
transitions (upstream fix vs. Rust refactor). Then refine the resulting model against `Tzimtzum`.
